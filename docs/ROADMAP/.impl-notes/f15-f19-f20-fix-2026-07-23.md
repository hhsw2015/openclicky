# F15 + F19 + F20 fixes — impl notes 2026-07-23

Everywhere pin: `30e03e9dcfdd4247fd679828ed86e9042f32d809`.

## F15 Bridge (`cursor-buddy/OpenClickyExternalControlBridge.swift`)

### HIGH — sensor_health.capture_count mismatch
- Line 2437: `"capture_count": sensorToolNames.count` → 43, but `sensorToolDescriptorsRaw` (line ~2338) appends `+ OpenClickyConnectorBridgeTools.descriptorsRaw + OpenClickyOpenCLIBridgeTools.descriptorsRaw`.
- Fix: replace with `sensorToolDescriptorsRaw.count` (dynamic; grows with future tools).
- Note: descriptors count is safer than sensorToolNames.count because it directly reflects `tools/list` output.

### MEDIUM — Port env override + fallback ladder
- Line 156: `init(port: UInt16 = 32123, ...)` — no env override or fallback bind.
- Fix: read `OPENCLICKY_MCP_PORT` env in resolver; try up to 10 fallback ports on bind failure.
- Everywhere pattern: `EverywhereMcpHttpOptions.cs:41-45`.

## F19 Stash writer + Hook

### HIGH #1 — sanitiseTokenValue missing space strip
- File: `Packages/OpenClickyContextService/Sources/OpenClickyContextService/Stash/OpenClickySanitiser.swift:68-72`
- Everywhere (`ContextStashWriter.cs:842`): `if (char.IsControl(c) || c == ' ' || c == '\t') continue;`
- Fix: add `c == " "` to skip clause.

### HIGH #2 — Hook systemMessage missing prefix
- File: `Packages/OpenClickyContextService/Sources/openclicky-context-hook/main.swift:113-117`
- Everywhere (`main.rs:115`): `msg = json_escape(&format!("✓ Everywhere context injected: {summary}"))`
- Fix: prepend `"✓ OpenClicky context injected: "` before summary.

### MEDIUM #1 — Envelope body order (hint vs JSON)
- File: `Packages/OpenClickyContextService/Sources/OpenClickyContextService/Stash/OpenClickyContextSnapshotPayload.swift:271-303`
- Everywhere (`ContextStashWriter.cs:677-732`): emits `[everywhere-ctx-json] ...\n` BEFORE hint block.
- Openclicky: emits hint before JSON, no trailing `\n` on JSON.
- Fix: reorder — JSON line + `\n` first, then hint block.

### MEDIUM #2 — JSON `.sortedKeys` vs insertion order
- File: same, line 311: `encoder.outputFormatting = [.sortedKeys]`
- Everywhere: System.Text.Json default (insertion order = C# record declaration).
- Fix: remove `.sortedKeys`; rely on Codable `CodingKeys` declaration order (already in same order as C# record).

### LOW — Generic hint copy paraphrase
- Line 296: `"If user's question needs pointer, ..."` — semantic drift.
- Everywhere: `"If the user's question needs more than this pointer, call the relevant Everywhere MCP tool — don't guess."`
- Fix: swap in Everywhere's exact text, rebranded (`OpenClicky` for `Everywhere`).

## F20 AppActivator

### HIGH #1 — fireLaunchPhrase awaited under writeLock (blocks capture single-flight)
- Files:
  - `cursor-buddy/OpenClickyContextStashWriter.swift:200-202` and `:350-352`
  - Both `await activateAgentAndFirePhrase()` inside writeLock scope (via `defer { writeLock.unlock() }` at :98/:298).
- Everywhere (`ContextStashWriter.cs:527`): `_ = Task.Run(async () => { ... })` — fire-and-forget.
- Fix: wrap in `Task { await activateAgentAndFirePhrase() }` at both call sites — detached, don't await.

### HIGH #2 — Missing Carbon SetFrontProcessWithOptions
- File: `cursor-buddy/OpenClickyAppActivator.swift:94-104`
- Everywhere (`MacAppActivator.cs:113-123,264-296`): calls `TryCarbonSetFront(pid)` as follow-up after `objc_msgSend_activate`.
- Fix: add Carbon fallback via `#if canImport(Carbon)` + `GetProcessForPID` + `SetFrontProcessWithOptions(&psn, kSetFrontProcessFrontWindowOnly=1)`. Best-effort; catch errors silently.

### MEDIUM #1 — Missing .activateIgnoringOtherApps
- File: `cursor-buddy/OpenClickyAppActivator.swift:100`
- Current: `[.activateAllWindows]`
- Everywhere: `ActivateAllWindows | ActivateIgnoringOtherApps` (0x01 | 0x02).
- Fix: `[.activateAllWindows, .activateIgnoringOtherApps]`.

### MEDIUM #2 — phrase.isEmpty vs isNullOrWhiteSpace
- Files:
  - `cursor-buddy/OpenClickyAppActivator.swift:139-140`
  - `cursor-buddy/OpenClickyContextStashWriter.swift:431-432`
- Everywhere: `IsNullOrWhiteSpace(phrase)`
- Fix: trim `.whitespacesAndNewlines` then `.isEmpty`. Rename `trimmedPhrase` to actually reflect the trim.

### LOW — Missing log on empty agentAppId
- File: `cursor-buddy/OpenClickyContextStashWriter.swift:424-425`
- Fix: `HeyClickyLog.log("openclicky.launch_phrase.no_agent_app", ...)`.
