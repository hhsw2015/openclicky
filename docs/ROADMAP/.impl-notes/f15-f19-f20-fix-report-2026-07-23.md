# F15 + F19 + F20 fix report — 2026-07-23

Everywhere pin: `30e03e9dcfdd4247fd679828ed86e9042f32d809`.

## Fixes applied

### F15 Bridge (`cursor-buddy/OpenClickyExternalControlBridge.swift`)

- **HIGH — sensor_health.capture_count now dynamic.** Replaced hard-coded `sensorToolNames.count` (43) with `sensorToolDescriptorsRaw.count` so the surface reported here always matches `tools/list` output. Live probe now returns 52 (native 43 + connector 6 + OpenCLI 3), grows automatically as F31/F32-F36 mount.
- **MEDIUM — Port env override + fallback ladder.** Added `resolveDefaultPort()` reading `OPENCLICKY_MCP_PORT` env (defaults 32123), plus `tryStart(port:fallbacksRemaining:)` walking up to 10 sibling ports on bind failure. Mirrors `EverywhereMcpHttpOptions.cs:41-45,11`.

### F19 Stash writer + Hook

- **HIGH #1 — sanitiseTokenValue drops space.** Added `c == " "` to the skip clause in `OpenClickySanitiser.swift`. Envelope grammar uses space as key=value terminator; missing this broke tokens with embedded spaces (`My App` exec basename, mailto URLs).
- **HIGH #2 — Hook systemMessage prefix.** `buildHookResponse` in `openclicky-context-hook/main.swift` now wraps summary as `"✓ OpenClicky context injected: <summary>"`. Matches Everywhere `main.rs:115` UX affordance.
- **MEDIUM #1 — Envelope order.** `formatForHook` in `OpenClickyContextSnapshotPayload.swift` now emits JSON line + trailing `\n` BEFORE the hint block, matching `ContextStashWriter.cs:677-732`.
- **MEDIUM #2 — JSON insertion order.** Removed `.sortedKeys` from JSONEncoder; relies on `Codable encode(to:)` field order (which matches C# record declaration in `ContextStashWriter.cs:963-990`).
- **LOW — Generic hint copy byte-ported.** All 5 hint branches now byte-port Everywhere strings verbatim (with only `everywhere` -> `openclicky` rebrand). Fixes semantic drift ("needs pointer" -> "needs more than this pointer").

### F20 AppActivator

- **HIGH #1 — fireLaunchPhrase is fire-and-forget.** Both call sites in `OpenClickyContextStashWriter.swift` (`:200-202` capture path, `:350-352` LinkRect path) now wrap `activateAgentAndFirePhrase()` in `Task { ... }` instead of awaiting inline. Releases the write single-flight immediately; matches Everywhere `Task.Run` at `ContextStashWriter.cs:527`.
- **HIGH #2 — Carbon SetFrontProcessWithOptions fallback.** Added `tryCarbonSetFront(pid:)` static in `OpenClickyAppActivator.swift`. Imports `ApplicationServices`, calls `GetProcessForPID` + `SetFrontProcessWithOptions(&psn, kSetFrontProcessFrontWindowOnly)` after the polite NSRunningApplication activate. Silenced 10.9 deprecation warning via `@available(macOS, deprecated: 10.9, ...)` on the wrapper. Ports `MacAppActivator.cs:113-123, 264-296`.
- **MEDIUM #1 — .activateIgnoringOtherApps flag added.** `app.activate(options:)` now passes `[.activateAllWindows, .activateIgnoringOtherApps]`. LSUIElement (menu-bar) apps don't inherit implicit ignore-other-apps semantics.
- **MEDIUM #2 — Whitespace-safe empty phrase check.** Both `OpenClickyAppActivator.fireLaunchPhrase` and `OpenClickyContextStashWriter.activateAgentAndFirePhrase` now trim `.whitespacesAndNewlines` before `.isEmpty`. Variable name `trimmedPhrase` now actually reflects a trim.
- **LOW — Log skip on empty agentAppId.** `activateAgentAndFirePhrase` in the writer now calls `HeyClickyLog.log("openclicky.launch_phrase.no_agent_app", ...)` before returning.

## Files touched

- `/Users/wowdd1/Dev/openclicky/cursor-buddy/OpenClickyExternalControlBridge.swift` (F15 HIGH + MEDIUM)
- `/Users/wowdd1/Dev/openclicky/cursor-buddy/OpenClickyContextStashWriter.swift` (F19 hook interop + F20 writeLock, empty-check, empty-log)
- `/Users/wowdd1/Dev/openclicky/cursor-buddy/OpenClickyAppActivator.swift` (F20 activate flags, Carbon fallback, phrase trim)
- `/Users/wowdd1/Dev/openclicky/Packages/OpenClickyContextService/Sources/OpenClickyContextService/Stash/OpenClickySanitiser.swift` (F19 HIGH #1)
- `/Users/wowdd1/Dev/openclicky/Packages/OpenClickyContextService/Sources/OpenClickyContextService/Stash/OpenClickyContextSnapshotPayload.swift` (F19 MEDIUM #1, #2, LOW copy)
- `/Users/wowdd1/Dev/openclicky/Packages/OpenClickyContextService/Sources/openclicky-context-hook/main.swift` (F19 HIGH #2)
- `/Users/wowdd1/Dev/openclicky/Packages/OpenClickyContextService/Tests/OpenClickyContextServiceTests/StashWriterTests.swift` (updated 3 assertions to match new byte-ported hint copy)
- `/Users/wowdd1/Dev/openclicky/scripts/test-mcp-sensor.sh` (relaxed hard 28/43 assertions to `>=` lower bounds so surface can grow without touching tests)

## Verification

- **SPM unit tests:** `cd Packages/OpenClickyContextService && swift test` -> 525 executed / 8 skipped / 0 failures / 0 unexpected. All 10 StashFormatter tests + 2 sanitiser tests still green.
- **Hook integration:** `bash scripts/test-context-hook.sh` -> 6/6 cases pass. Manual probe confirms new `"✓ OpenClicky context injected: ..."` prefix in systemMessage.
- **MCP sensor integration:** `OPENCLICKY_BRIDGE_TOKEN=... bash scripts/test-mcp-sensor.sh` -> ALL SENSOR MCP TESTS PASSED. `sensor_health` returned `{status:ok, capture_count:52}` (dynamic; was 43 hard-coded before).
- **Xcode build:** intentionally skipped per project rule "Do not run xcodebuild from the terminal." All modified sources pass `swiftc -parse`.

## Notes / follow-ups (out of scope)

- Task item mentioned issue "captureLinks redacts credentials — Everywhere does not." Left unchanged as documented OpenClicky improvement per review report Issue 6.
- F19 Issue 5 (`URLComponents.string` vs `UriBuilder.AbsoluteUri` byte-parity nit) not fixed; harmless edge case.
- F20 Issue 5 (frontmost recheck exception-safety) not needed — Swift accessors don't throw today.
- Sensor test script now uses `>=` for tool counts to remain future-proof as tool surface grows.
