# F06 Terminal scrollback via AXValue - code review

Everywhere source pinned at `30e03e9dcfdd4247fd679828ed86e9042f32d809`.
Reviewed strictly against code; no doc-only claims trusted.

## Sources
- openclicky: `Packages/OpenClickyContextService/Sources/OpenClickyContextService/Capture/TerminalCapture.swift`
- openclicky types: `Packages/OpenClickyContextService/Sources/OpenClickyContextService/Types/CaptureTypes.swift:805-835`
- Everywhere: `/Users/wowdd1/Dev/Everywhere/src/Everywhere.Mcp/Tools/GetTerminalOutputTool.cs`

## Checklist verification

### 1. `linesBack` clamp `[1, 10_000]`, default 200 - PASS
- `TerminalCapture.swift:53` `defaultLinesBack: Int = 200`
- `TerminalCapture.swift:56` `maxLinesBack: Int = 10_000`
- `TerminalCapture.swift:97` signature: `capture(linesBack: Int = defaultLinesBack)`
- `TerminalCapture.swift:99` `let maxLines = max(1, min(linesBack, maxLinesBack))`
- Everywhere `GetTerminalOutputTool.cs:12-13, 27` `Math.Clamp(lines_back ?? DefaultLinesBack, 1, MaxLinesBack)` -> matches byte-for-byte semantically.

### 2. 200-byte avg line cap - PASS
- `TerminalCapture.swift:61` `averageLineCapBytes: Int = 200`
- `TerminalCapture.swift:133` `let maxBytes = maxLines * averageLineCapBytes`
- `TerminalCapture.swift:134` `readAXText(focused, maxLength: maxBytes)`
- Everywhere `GetTerminalOutputTool.cs:16, 42-43` identical constant and multiplication.

### 3. 8-substring terminal executable heuristic - PASS (byte match)
`TerminalCapture.swift:66-75` needle list vs Everywhere `GetTerminalOutputTool.cs:67-74`:

| # | openclicky | Everywhere | match |
|---|------------|------------|-------|
| 1 | `"term"` | `"term"` | yes |
| 2 | `"iterm"` | `"iterm"` | yes |
| 3 | `"ghostty"` | `"ghostty"` | yes |
| 4 | `"warp"` | `"warp"` | yes |
| 5 | `"alacritty"` | `"alacritty"` | yes |
| 6 | `"kitty"` | `"kitty"` | yes |
| 7 | `"konsole"` | `"konsole"` | yes |
| 8 | `"xterm"` | `"xterm"` | yes |

Count = 8. Case handling: openclicky lowercases via `appKey.lowercased()` at line 163 and uses `contains`. Everywhere uses `StringComparison.OrdinalIgnoreCase`. Equivalent because `AppKeyResolver.fromProcessId` already lowercases (comment line 160-162).

### 4. Warp false-negative preserved - PASS
- Comment `TerminalCapture.swift:20-22, 70` explicitly documents Warp's binary is `stable` and the `"warp"` needle rarely fires.
- No workaround added; ported as-is per instructions.

### 5. Empty-buffer C# quirk `"".Split('\n').Length == 1` - PASS
- `TerminalCapture.swift:138` uses `split(separator: "\n", omittingEmptySubsequences: false)`.
- On empty buffer path: `readAXText` returns nil for empty text (line 213), coalesced to `""` at line 134. `"".split(...omittingEmptySubsequences: false)` yields `[""]` (count 1) -> `linesReturned: 1, text: ""`.
- Also handled explicitly at line 128 for the "focused element missing" branch.
- CaptureTypes.swift:797-799 documents this quirk on the type.

### 6. JSON keys `is_terminal / lines_returned / text` - PASS
`CaptureTypes.swift:830-834`:
```
case isTerminal = "is_terminal"
case linesReturned = "lines_returned"
case text
```
Matches Everywhere `GetTerminalOutputTool.cs:34-36, 48-50` anonymous object property names.

## Additional deviations found (documented in source)

1. `TerminalCapture.swift:25-29` - AX failure returns `nil` instead of Everywhere's `ToolErrors.FromException`. Deliberate; capture API has no error shape.
2. `TerminalCapture.swift:30-34` - `String.count` (grapheme) vs C# `.Length` (UTF-16). Only affects the byte-cap boundary; Swift's slicing preserves grapheme boundaries. Safe.
3. `TerminalCapture.swift:35-38` - method is `async` for sibling API parity; body currently synchronous. No behavioural impact.
4. `TerminalCapture.swift:203-218` - `readAXText` restricted to AXValue-only path (no AXRow/AXTableRow/AXOutlineRow flattening). Documented at 197-201 as correct for terminals.

## Verdict

All F06 acceptance criteria pass. Port is a 1:1 semantic mirror of `GetTerminalOutputTool.cs`. All divergences are documented in-source with rationale. Warp false-negative is preserved intentionally.
