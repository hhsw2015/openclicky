// OpenRewindKit — public namespace + version. Individual types live in
// sibling files: Reader.swift, Writer.swift, Compressor.swift, etc.

public enum OpenRewindKit {
    public static let version = "0.1.0-alpha"
}
